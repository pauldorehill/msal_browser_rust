{
  "scripts": {
    "build": "del dist/js && rollup --config",
    "start": "del dist/js && rollup --config --watch"
  },
  "devDependencies": {
    "@azure/msal-browser": "^2.0.0",
    "@wasm-tool/rollup-plugin-rust": "file:../rollup-plugin-rust",
    "del-cli": "^3.0.1",
    "rollup": "^2.23.0",
    "rollup-plugin-livereload": "^1.3.0",
    "rollup-plugin-serve": "^1.0.3"
  }
}
