{
  "type": "module",
  "scripts": {
    "build": "rimraf dist/js && rollup -c",
    "start": "rimraf dist/js && rollup -c -w"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.1.0",
    "@wasm-tool/rollup-plugin-rust": "^2.3.2",
    "rimraf": "^3.0.2",
    "rollup": "^3.3.0",
    "rollup-plugin-livereload": "^2.0.5",
    "rollup-plugin-serve": "^2.0.1"
  }
}